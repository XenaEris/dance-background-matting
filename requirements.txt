numpy==1.26.4
Pillow>=10.0
opencv-contrib-python-headless==4.10.0.84

# Optional SAM2 experiment dependencies. Keep torch/torchvision pinned by the
# active environment because the existing RVM demo depends on the installed CUDA build.
sam2==1.1.0
hydra-core>=1.3
iopath>=0.1.10
huggingface_hub>=0.23
tqdm>=4.66
